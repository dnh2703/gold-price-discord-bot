name: Deploy Discord Bot to Railway

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Setup Bun
      uses: oven-sh/setup-bun@v1
      with:
        bun-version: latest
    
    - name: Install dependencies
      run: bun install
    
    - name: Build project
      run: bun run build
    
    - name: Run tests
      run: bun test
      
  deploy:
    needs: test
    runs-on: ubuntu-latest
    # Only deploy on pushes to main branch (not PRs)
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    
    # Use Railway's official CLI container for consistency
    container: ghcr.io/railwayapp/cli:latest
    
    env:
      RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
      DISCORD_TOKEN: ${{ secrets.DISCORD_TOKEN }}
      CHANNEL_ID: ${{ secrets.CHANNEL_ID }}
      TIMEZONE: ${{ secrets.TIMEZONE }}
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Verify Railway token
      run: |
        if [ -z "$RAILWAY_TOKEN" ]; then
          echo "❌ RAILWAY_TOKEN secret is not set"
          echo "Please add your Railway project token to GitHub repository secrets"
          exit 1
        fi
        echo "✅ Railway token is configured"

    - name: Connect to Railway project
      run: |
        echo "🔗 Connecting to Railway project..."
        # The RAILWAY_TOKEN should be automatically used by the CLI
        railway whoami || echo "⚠️ Could not verify Railway authentication"
        railway status || echo "⚠️ Could not get project status"
        
    - name: Check Railway project info
      run: |
        echo "🔍 Checking Railway project information..."
        railway status || echo "⚠️ Could not get project status"
        railway --help || echo "⚠️ Could not get Railway help"

    - name: Set Railway Environment Variables
      run: |
        echo "🔧 Setting environment variables in Railway..."
        
        # Set environment variables in Railway using modern syntax
        if [ -n "$DISCORD_TOKEN" ]; then
          railway variables --set DISCORD_TOKEN="$DISCORD_TOKEN" || railway env set DISCORD_TOKEN="$DISCORD_TOKEN" || echo "⚠️ Failed to set DISCORD_TOKEN"
          echo "✅ DISCORD_TOKEN set in Railway"
        else
          echo "⚠️ DISCORD_TOKEN not found in GitHub Secrets"
        fi
        
        if [ -n "$CHANNEL_ID" ]; then
          railway variables --set CHANNEL_ID="$CHANNEL_ID" || railway env set CHANNEL_ID="$CHANNEL_ID" || echo "⚠️ Failed to set CHANNEL_ID"
          echo "✅ CHANNEL_ID set in Railway"
        else
          echo "⚠️ CHANNEL_ID not found in GitHub Secrets"
        fi
        
        if [ -n "$TIMEZONE" ]; then
          railway variables --set TIMEZONE="$TIMEZONE" || railway env set TIMEZONE="$TIMEZONE" || echo "⚠️ Failed to set TIMEZONE"
          echo "✅ TIMEZONE set in Railway"
        else
          echo "ℹ️ TIMEZONE not set, using default"
        fi

    - name: Deploy to Railway
      run: |
        echo "🚀 Starting deployment to Railway..."
        
        # Check Railway CLI version and available commands
        echo "🔍 Railway CLI version:"
        railway --version || echo "Could not get version"
        
        # Try the modern Railway deployment approach
        echo "📋 Attempting deployment..."
        
        # Method 1: Try railway up (modern syntax)
        if railway up --detach; then
          echo "✅ Deployment successful using railway up!"
        else
          echo "⚠️ railway up failed, trying railway deploy..."
          
          # Method 2: Try railway deploy
          if railway deploy --detach; then
            echo "✅ Deployment successful using railway deploy!"
          else
            echo "⚠️ railway deploy failed, trying without detach..."
            
            # Method 3: Try without detach flag
            if railway up; then
              echo "✅ Deployment successful!"
            else
              echo "❌ All deployment methods failed"
              echo "Railway status:"
              railway status
              exit 1
            fi
          fi
        fi
        
        echo "✅ Deployment completed!"
        echo "🔗 Check your Railway dashboard for deployment status"
    
    - name: Deployment notification
      if: success()
      run: |
        echo "🎉 Discord Bot deployed successfully to Railway!"
        echo "The bot should be running and ready to serve gold price updates."
    
    - name: Deployment failure notification
      if: failure()
      run: |
        echo "❌ Deployment failed!"
        echo "Please check the logs above and your Railway dashboard for more details." 